# TODO

## To review

- **`test-songs.csv` and `test_data.csv` in repo root** — Two stray test CSV files in the repo root. No code or docs reference them. Decide whether they are still useful for manual testing, should be moved into `tests/`, or removed.
