---
phase: 12-devcontainer-setup
plan: 01
type: execute
---

<objective>
Configure devcontainer with Claude Code feature for sandbox-free development.

Purpose: Enable Claude Code to run without sandbox restrictions inside container
Output: Working devcontainer configuration with .NET 10 + Node.js + Claude Code
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Project Stack:**
- .NET 10 Minimal API backend
- Svelte 5 + Tailwind v4 frontend (Node.js/npm required)
- Ports: 5657 (.NET API), 5173 (Vite dev server)

**User Choice:** Simple setup without firewall restrictions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create devcontainer.json with Claude Code feature</name>
  <files>.devcontainer/devcontainer.json</files>
  <action>
Create devcontainer.json with:
- Base image: mcr.microsoft.com/devcontainers/dotnet:1-10.0
- Node.js feature for frontend builds
- Claude Code feature from anthropics/devcontainer-features
- Volume mount for Claude config persistence (~/.claude)
- Forward ports 5657 and 5173
- VS Code extensions: C#, Svelte, Tailwind CSS, Claude Code
- postCreateCommand: npm install in web/ directory
  </action>
  <verify>File exists at .devcontainer/devcontainer.json with valid JSON</verify>
  <done>devcontainer.json created with all required features and mounts</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Devcontainer configuration for Claude Code development</what-built>
  <how-to-verify>
    1. Open VS Code in project directory
    2. Press Ctrl+Shift+P → "Dev Containers: Reopen in Container"
    3. Wait for container to build (first time takes ~2-3 min)
    4. In container terminal, verify:
       - `dotnet --version` shows 10.x
       - `node --version` shows Node.js available
       - `claude --version` shows Claude Code CLI installed
    5. Run: `dotnet run` - should build frontend and start server
    6. Access http://localhost:5657 in browser
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] .devcontainer/devcontainer.json exists and is valid JSON
- [ ] Container builds without errors
- [ ] dotnet, node, and claude CLIs are available
- [ ] `dotnet run` successfully builds and serves the app
</verification>

<success_criteria>
- devcontainer.json created with Claude Code feature
- Container builds and runs successfully
- All required tools (dotnet, node, claude) available in container
- Claude config persists via volume mount
</success_criteria>

<output>
After completion, create `.planning/phases/12-devcontainer-setup/12-01-SUMMARY.md`
</output>
