---
phase: 20-remove-ci-cd
plan: 01
type: execute
---

<objective>
Remove CI/CD workflow and create environment variable documentation.

Purpose: Eliminate dependency on GitHub Actions and GHCR - users build locally from source.
Output: Clean repo with no CI/CD artifacts, clear .env.example for setup guidance.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-remove-ci-cd/20-CONTEXT.md

**Phase Context (from /gsd:discuss-phase):**
- Vision: Clean slate - delete CI/CD entirely, users clone + docker compose build
- Essential: Clear .env setup, no traces of old CI/CD
- Out of scope: README updates (Phase 21)

**Relevant files:**
@.github/workflows/release.yml
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete CI/CD workflow and directory</name>
  <files>.github/workflows/release.yml, .github/workflows/, .github/</files>
  <action>
    1. Delete .github/workflows/release.yml
    2. Remove .github/workflows/ directory (now empty)
    3. Remove .github/ directory (now empty)

    This removes the entire GitHub Actions CI/CD pipeline that was publishing to GHCR.
  </action>
  <verify>ls -la .github/ 2>&1 should return "No such file or directory"</verify>
  <done>.github/ directory no longer exists, CI/CD pipeline completely removed</done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example with documented variables</name>
  <files>.env.example</files>
  <action>
    Create .env.example in project root with:

    ```
    # Hitster Card Generator Environment Variables
    # Copy this file to .env and fill in your values

    # Required: Spotify API Credentials
    # Get these from https://developer.spotify.com/dashboard
    SPOTIFY_CLIENT_ID=your_client_id_here
    SPOTIFY_CLIENT_SECRET=your_client_secret_here

    # Optional: Container settings (for docker-compose)
    # TZ=Europe/Amsterdam
    # PUID=1000
    # PGID=1000
    ```

    Format: Required variables uncommented with placeholder values, optional variables commented with defaults.
  </action>
  <verify>cat .env.example shows both required Spotify vars and optional container vars</verify>
  <done>.env.example exists with documented required (Spotify) and optional (PUID/PGID/TZ) variables</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `.github/` directory does not exist
- [ ] `.env.example` exists in project root
- [ ] `.env.example` contains SPOTIFY_CLIENT_ID and SPOTIFY_CLIENT_SECRET as required
- [ ] `.env.example` contains TZ, PUID, PGID as optional (commented)
- [ ] `git status` shows deleted workflow and new .env.example
</verification>

<success_criteria>

- CI/CD workflow completely removed
- .env.example provides clear setup guidance
- No orphaned references to old CI/CD in modified files (README is Phase 21)
- Repository ready for local-build-only workflow
</success_criteria>

<output>
After completion, create `.planning/phases/20-remove-ci-cd/20-01-SUMMARY.md`:

# Phase 20 Plan 01: Remove CI/CD Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for Phase 21: Local Build Docs (README updates)
</output>
