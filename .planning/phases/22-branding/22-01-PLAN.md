---
phase: 22-branding
plan: 01
type: execute
---

<objective>
Add branded gradient favicon and update main page music note with rainbow gradient.

Purpose: Replace default Vite favicon with branded music note and enhance landing page visual identity.
Output: Gradient favicon SVG, updated index.html, updated App.svelte with SVG music note.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Current state:**
- Favicon: `web/public/vite.svg` (default Vite icon)
- index.html references `/vite.svg` as favicon
- Landing page uses emoji `🎵` with bounce animation (App.svelte line 233)

**Visual verification:**
Use Chrome DevTools MCP tools to verify changes visually.

@web/index.html
@web/src/App.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gradient favicon SVG</name>
  <files>web/public/favicon.svg, web/index.html</files>
  <action>
    1. Create `web/public/favicon.svg` with a music note icon using rainbow gradient
       - Use linearGradient with stops for rainbow colors (red, orange, yellow, green, blue, purple)
       - Simple beamed eighth notes or single quarter note silhouette
       - Ensure it looks good at 32x32 and 16x16 sizes (keep it simple, bold shapes)
    2. Update `web/index.html`:
       - Change favicon href from `/vite.svg` to `/favicon.svg`
       - Update title from "web" to "Hitster Card Generator"
    3. Keep vite.svg for now (don't delete, may be used by Vite)
  </action>
  <verify>favicon.svg exists, index.html references it, file is valid SVG</verify>
  <done>Favicon SVG created with rainbow gradient, index.html updated</done>
</task>

<task type="auto">
  <name>Task 2: Update main page music note with gradient SVG</name>
  <files>web/src/App.svelte</files>
  <action>
    1. Replace the emoji music note on landing page (line ~233: `<div class="text-8xl animate-bounce">🎵</div>`)
    2. Create inline SVG music note with rainbow gradient matching the favicon style
    3. Apply the existing `animate-bounce` class to the SVG container
    4. Size appropriately (~128px to match text-8xl scale)
    5. Use same rainbow gradient colors as favicon for consistency
  </action>
  <verify>App.svelte contains SVG music note instead of emoji, gradient defined</verify>
  <done>Landing page displays animated SVG music note with rainbow gradient</done>
</task>

<task type="auto">
  <name>Task 3: Verify visuals with Chrome DevTools</name>
  <files>None (verification only)</files>
  <action>
    1. Start the dev server: `dotnet run` or use existing running server
    2. Navigate to localhost:5657 (or 5173 for Vite dev)
    3. Use `mcp__chrome-devtools__take_screenshot` to capture the landing page
    4. Verify:
       - Browser tab shows the gradient music note favicon
       - Landing page displays the rainbow gradient music note (not emoji)
       - Animation is smooth (bounce effect working)
    5. Take screenshot of browser tab to confirm favicon appears correctly
  </action>
  <verify>Screenshots show gradient favicon in tab and rainbow music note on page</verify>
  <done>Visual verification confirms both branding elements display correctly</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `favicon.svg` exists in `web/public/` with rainbow gradient
- [ ] `index.html` references `/favicon.svg` and has correct title
- [ ] `App.svelte` has SVG music note instead of emoji
- [ ] Visual screenshot confirms favicon in browser tab
- [ ] Visual screenshot confirms gradient music note on landing page
- [ ] `npm run build` succeeds (frontend builds without errors)
</verification>

<success_criteria>

- Gradient favicon displays in browser tab
- Landing page shows animated rainbow gradient music note
- Both elements use consistent rainbow color scheme
- No build errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/22-branding/22-01-SUMMARY.md`:

# Phase 22 Plan 01: Branding Summary

**[One-liner describing what shipped]**

## Accomplishments

- Created gradient favicon with rainbow music note
- Updated landing page music note to SVG with rainbow gradient
- Verified visuals with Chrome DevTools screenshots

## Files Created/Modified

- `web/public/favicon.svg` - New gradient favicon
- `web/index.html` - Updated favicon reference and title
- `web/src/App.svelte` - SVG music note replacing emoji

## Decisions Made

[Any decisions about gradient colors, icon style, etc.]

## Issues Encountered

[Any issues, or "None"]

## Next Step

Phase 22 complete. Ready for next milestone.
</output>
