---
phase: 16-multi-arch-docs
plan: 02
type: execute
---

<objective>
Create comprehensive README.md that serves end users, admins, and developers.

Purpose: Make the project welcoming and easy to understand for anyone discovering it - from quick docker-compose setup to understanding the architecture.
Output: Complete README.md with logo, badges, quick start, screenshots, deployment examples, environment variables, and developer documentation.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-multi-arch-docs/16-CONTEXT.md
@.planning/phases/16-multi-arch-docs/16-RESEARCH.md

**From CONTEXT.md - Vision:**
- README is the star - comprehensive, friendly document for three audiences
- Quick start at top, details below for depth
- Friendly casual tone - like explaining to a friend
- Both docker-compose examples embedded in README (no separate files)

**From CONTEXT.md - Essential:**
- Two compose patterns (simple + Traefik), well-commented
- Full workflow screenshots (Upload → Match → Preview → Export)
- Rainbow music note logo at top
- Standard GitHub badges (build status, license, Docker pulls)

**From CONTEXT.md - Boundaries:**
- No Kubernetes - just docker-compose
- No SSL/TLS config details - Traefik handles that
- No CONTRIBUTING.md - personal project
- No step-by-step Spotify setup - just mention env vars

**From CONTEXT.md - Structure:**
Logo + badges → Quick start → Screenshots → Deployment (simple + Traefik) → Environment variables → Developer section (architecture + local dev)

**From RESEARCH.md - Docker Compose Examples:**
Simple (direct ports):
```yaml
services:
  hitster:
    image: ghcr.io/sixfive7/histercardgenerator:latest
    container_name: hitster
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      SPOTIFY_CLIENT_ID: your_client_id_here
      SPOTIFY_CLIENT_SECRET: your_client_secret_here
```

Traefik (no port exposure):
```yaml
services:
  hitster:
    image: ghcr.io/sixfive7/histercardgenerator:latest
    container_name: hitster
    restart: unless-stopped
    environment:
      SPOTIFY_CLIENT_ID: your_client_id_here
      SPOTIFY_CLIENT_SECRET: your_client_secret_here
    labels:
      traefik.enable: true
      traefik.http.services.hitster.loadbalancer.server.port: 8080
```

**Relevant source files:**
@example.yaml (Traefik pattern reference - user's personal file)
@Dockerfile
@HitsterCardGenerator/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive README.md</name>
  <files>README.md, docs/</files>
  <action>
Create README.md with the following structure:

1. **Header section:**
   - Rainbow music note emoji logo (use colorful music emojis as ASCII art alternative, or simple text header)
   - Project title: "Hitster Card Generator"
   - One-line description: Generate printable QR code cards for your custom Hitster music game
   - GitHub badges: build status, license (MIT), Docker pulls from GHCR

2. **Quick Start section:**
   - Minimal docker run or docker-compose example to get running fast
   - Just the essentials - point to detailed sections below

3. **Screenshots section:**
   - Header indicating workflow screenshots
   - Placeholder image references in `docs/screenshots/` directory:
     - `docs/screenshots/01-upload.png` - CSV upload with drag-drop
     - `docs/screenshots/02-match.png` - Spotify matching with confidence scores
     - `docs/screenshots/03-preview.png` - Card carousel with color customization
     - `docs/screenshots/04-export.png` - PDF export with cutting lines
   - Create the `docs/screenshots/` directory (empty, for user to add images)

4. **Deployment section:**
   - **Simple Setup** - docker-compose with direct port mapping (8080:8080)
   - **Traefik Setup** - docker-compose with Traefik labels, no port exposure
   - Both examples well-commented, copy-paste ready
   - Mention image supports AMD64 and ARM64 (Apple Silicon, Raspberry Pi)

5. **Environment Variables section:**
   - Table format: Variable | Required | Description
   - SPOTIFY_CLIENT_ID - Required - Your Spotify API client ID
   - SPOTIFY_CLIENT_SECRET - Required - Your Spotify API client secret
   - Brief note: Get credentials from Spotify Developer Dashboard

6. **Developer section:**
   - **Architecture overview:** .NET 10 Minimal API backend, Svelte 5 + Tailwind frontend, QuestPDF for PDF generation
   - **Local development:** Prerequisites (.NET 10, Node.js), devcontainer option, how to run (dotnet run + npm run dev)
   - **Tech stack:** Brief list of key libraries (SpotifyAPI.Web, QRCoder, QuestPDF)

7. **License section:**
   - MIT License link

Tone: Friendly, casual, helpful. Like explaining to a friend who wants to set this up.
  </action>
  <verify>
- `test -f README.md && echo "README exists"`
- `test -d docs/screenshots && echo "Screenshots dir exists"`
- `grep -q "docker-compose" README.md && echo "Has compose examples"`
- `grep -q "SPOTIFY_CLIENT" README.md && echo "Has env vars"`
  </verify>
  <done>README.md exists with all sections, docs/screenshots/ directory created, both compose examples included</done>
</task>

<task type="auto">
  <name>Task 2: Capture workflow screenshots using Chrome DevTools</name>
  <files>docs/screenshots/</files>
  <action>
Use the Chrome DevTools MCP to capture screenshots of the app workflow:

1. Start the app in background (dotnet run)
2. Navigate Chrome to http://localhost:5657
3. Capture screenshots at each workflow step:

   a. **01-upload.png** - Initial upload page with drag-drop zone
      - Navigate to app root
      - Take screenshot, save to docs/screenshots/01-upload.png

   b. **02-match.png** - Spotify matching results
      - Upload a sample CSV (or use existing test data)
      - Wait for matching to complete
      - Take screenshot showing confidence scores

   c. **03-preview.png** - Card carousel preview
      - Navigate to preview/cards section
      - Take screenshot showing card with color options

   d. **04-export.png** - PDF export section
      - Navigate to export section
      - Take screenshot showing export options

Use mcp__chrome-devtools__take_screenshot with filePath parameter to save directly to docs/screenshots/.
Resize page to consistent dimensions (e.g., 1280x800) for uniform screenshots.
  </action>
  <verify>
- `ls docs/screenshots/*.png | wc -l` shows 4 files
- All screenshot files are valid PNGs
  </verify>
  <done>All four workflow screenshots captured and saved to docs/screenshots/</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] README.md exists with all required sections
- [ ] docs/screenshots/ directory exists
- [ ] README has both docker-compose examples (simple + Traefik)
- [ ] README has environment variables table
- [ ] README has developer section with architecture and local dev
- [ ] Screenshots captured via Chrome DevTools
</verification>

<success_criteria>
- All tasks completed
- README.md is comprehensive and welcoming
- Documentation serves all three audiences (users, admins, developers)
- Phase 16 complete, v2.3 Containerization milestone ready to ship
</success_criteria>

<output>
After completion, create `.planning/phases/16-multi-arch-docs/16-02-SUMMARY.md`

Update STATE.md:
- Phase: 16 of 16 complete
- Progress: 100%
- Status: v2.3 Containerization milestone complete
</output>
